{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main CrewHub windows",
  "windows": ["main", "chat", "world"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
